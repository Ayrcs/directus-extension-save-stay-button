{
  "name": "directus-extension-save-stay-button",
  "version": "1.0.0",
  "description": "Directus interface button that triggers the native save shortcut and stays on the current page.",
  "icon": "save",
  "type": "module",
  "files": [
    "dist"
  ],
  "directus:extension": {
    "type": "interface",
    "path": "dist/index.js",
    "source": "src/index.js",
    "host": "^12.0.0"
  },
  "scripts": {
    "build": "directus-extension build",
    "dev": "directus-extension build -w --no-minify",
    "link": "directus-extension link"
  },
  "devDependencies": {
    "@directus/extensions-sdk": "^12.0.2",
    "vue": "^3.5.3"
  }
}
